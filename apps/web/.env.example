# Path to the SQLite DB written by the Python engine
DATABASE_PATH=../../kronos_trade.db

# Python FastAPI engine base URL (server-side rewrites + WebSocket)
ENGINE_URL=http://localhost:8765

# Exposed to browser for WebSocket connection
NEXT_PUBLIC_WS_URL=ws://localhost:8765/ws
NEXT_PUBLIC_ENGINE_URL=http://localhost:8765
